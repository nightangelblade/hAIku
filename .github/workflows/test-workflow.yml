name: Test Workflow

on:
  push:
    branches:
      - 'auto_haiku_generate**'

jobs:
  testing-workflow:
    runs-on: ubuntu-latest
    steps:
      - name: Echo that the workflow is working
        run: echo "Hello World!"
